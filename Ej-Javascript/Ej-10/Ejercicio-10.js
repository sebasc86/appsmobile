let array = [1, 2, 3, 4, 5, 6, 20];

const even = (element, arr) => { 
    result = arr.indexOf(element)
  
    if(result !== -1){
        return (true);
    }

    return(false);
      
};

console.log(even(9, array));